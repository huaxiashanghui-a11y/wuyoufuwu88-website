# 北苍星际速充 - 数据同步指南

## 🔄 前后端数据同步流程

### 工作原理

1. **管理后台** (admin-v2-final.html) - 添加/编辑商品，数据保存在 localStorage
2. **导出功能** - 从管理后台导出 JSON 数据
3. **更新 data.js** - 将导出的数据复制到 data.js 文件
4. **前端自动加载** - index.html 加载 data.js，显示最新数据

---

## 📝 具体操作步骤

### 第一步：在管理后台添加/修改商品

1. 访问管理后台：https://huaxiashanghui-a11y.github.io/wuyoufuwu88-website/admin-v2-final.html
2. 登录（密码：beicang888）
3. 进入"商品管理"页面
4. 添加/编辑/删除商品

### 第二步：导出数据

1. 在管理后台点击右上角 **"📥 导出数据"** 按钮
2. 会自动下载一个 `beicang-admin-data.json` 文件

### 第三步：更新 data.js

**方法A：使用 OpenClaw 自动更新（推荐）**

在聊天中输入：
```
更新网站数据
```
我会自动：
1. 读取导出的 JSON 文件
2. 更新 data.js
3. 推送到 GitHub

**方法B：手动更新**

1. 打开下载的 `beicang-admin-data.json` 文件
2. 复制 JSON 内容
3. 打开 `/home/openclaw/.openclaw/workspace/data.js`
4. 找到 `let sharedProducts = [...]` 这一行
5. 替换方括号内的所有内容为导出的数据
6. 保存文件

### 第四步：推送到 GitHub

更新完 data.js 后，运行以下命令：

```bash
cd /home/openclaw/.openclaw/workspace
git add data.js
git commit -m "更新商品数据"
git push origin main
```

或者告诉我"推送到 GitHub"，我来帮你完成。

---

## 🎯 快速操作命令

在聊天中可以直接使用：

- **"更新网站数据"** - 我会帮你完成所有数据同步步骤
- **"推送数据到 GitHub"** - 推送更新后的数据到线上
- **"查看当前商品"** - 列出当前所有商品
- **"添加商品 [商品名]"** - 添加新商品（在管理后台操作）

---

## ⚠️ 注意事项

1. **首次使用** - 先在管理后台添加商品，再导出更新
2. **备份习惯** - 导出数据后，保存备份到本地
3. **缓存问题** - 更新后如看不到效果，按 `Ctrl+F5` 强制刷新
4. **数据格式** - 不要手动编辑 data.js，使用管理后台导出功能

---

## 📂 文件说明

### 核心文件

- **`data.js`** - 前端数据源（从管理后台导出更新）
- **`index.html`** - 前端页面（自动加载 data.js）
- **`admin-v2-final.html`** - 管理后台（数据保存在 localStorage）
- **`data.json`** - 数据备份文件（自动生成）

### 文件关系

```
管理后台 (localStorage)
    ↓ [导出]
beicang-admin-data.json
    ↓ [复制/更新]
data.js
    ↓ [加载]
index.html
    ↓ [显示]
用户看到最新商品
```

---

## 🔧 故障排查

### 问题1：更新数据后前端没变化

**解决方法：**
1. 检查 data.js 是否正确更新
2. 清除浏览器缓存（Ctrl+Shift+Delete）
3. 强制刷新页面（Ctrl+F5）
4. 检查 GitHub 是否部署完成（等待1-3分钟）

### 问题2：导出的数据格式不对

**解决方法：**
1. 确保在管理后台正确操作
2. 检查 JSON 文件内容格式
3. 重新导出数据

### 问题3：无法加载 data.js

**解决方法：**
1. 检查 data.js 文件是否存在
2. 检查文件路径是否正确
3. 查看浏览器控制台错误信息

---

## 📞 技术支持

遇到问题？
- **Telegram:** https://t.me/bcsz88
- **电话:** 09688337441
- **工作时间:** 9:30-24:00

---

**最后更新：** 2026-03-15
